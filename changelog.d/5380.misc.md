Clarified in Settings UI descriptions that `search.final_max_results` and `search.cross_engine_max_results` apply to pipeline/source-based strategies, while autonomous agent strategies (`langgraph-agent`) manage context autonomously.
