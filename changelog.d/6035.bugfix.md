The MCP server now emits the log records the rest of the package writes, and strips control characters from them. The package disables its own loguru namespace on import and only the web path re-enabled it, so the MCP subprocess logged nothing of its own; and without the sanitizer a research query containing a newline could forge a second stderr log record.
