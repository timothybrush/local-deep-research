Collection search now checks eligibility with an index-scoped existence query instead of the full RAG statistics query it used before -- 3 statements instead of 5 on an exact index match. The "indexed under other configurations" diagnostic that explains an unexpectedly empty collection is still logged.
