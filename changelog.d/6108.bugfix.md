Uploads (PDF and collection uploads of .txt/.docx/.epub) now receive a deterministic safe fallback name when a stem containing non-Latin letters or digits loses all characters during ASCII sanitization. Over-long filenames that only fit an extension, or leave no room for one, are now rejected instead of being silently truncated past the length limit.
