The topic-organization research strategy now routes its eight model calls (topic extraction, lead selection, relevance filtering, reorganization, refinement questions, text and summary synthesis) through a single `_invoke_model` helper on the synchronous client, so response normalization happens in one place; an additive `_ainvoke_model` twin awaits `ainvoke` for the future single-loop async runner (#6293). Fourth tranche of #5854.
