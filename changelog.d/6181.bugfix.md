Settings redaction: the container write-back no-op guard now compares non-string leaves by exact type as well as value, so a cross-type edit (e.g. an integer replaced by a boolean or float) inside a redacted sensitive container is no longer misclassified as an untouched round trip.
