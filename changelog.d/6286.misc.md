Search-engine query optimizers, the full-text search URL quality filter, report generation, domain classification and in-process document analysis now use the shared synchronous LLM dispatcher. Prompt forwarding, response parsing and exception handling are preserved.
