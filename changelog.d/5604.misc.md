Clearer guidance when a self-hosted SearXNG URL is blocked by the private-URL guard: the runtime self-disable now logs at ERROR (was WARNING) and both it and the settings-save rejection name all three operator remedies; docs, installation guides, and `.env.template` now document the v1.10.3 guard next to every `localhost:8080` example instead of describing the old allow-by-default behavior.
