Quarto export now escapes the report title to YAML double-quoted scalar rules before interpolating it into the .qmd front matter: quotes, backslashes, newlines, C0 and C1 control characters, and the Unicode line separators U+2028/U+2029 in web-derived or caller-supplied titles can no longer break front-matter parsing or inject YAML keys.
