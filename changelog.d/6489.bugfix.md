Fix propagation of `search.snippets_only`, `search.max_filtered_results`, `search.region`, `search.safe_search`, and `search.search_language` when search engines are instantiated directly via `create_search_engine` (such as in autonomous agent tools and MCP). Default `search_snippets_only` to `None` in `get_search()` so callers that omit it do not silently override engine defaults. Fix `BaseCitationHandler` document generation to fall back to snippet when `full_content` is `None`. Fix `SourceBasedSearchStrategy` to toggle `search_snippets_only` when toggling `include_text_content`, ensuring topic-organization sub-searches genuinely force snippets-only on unwrapped search engines.
