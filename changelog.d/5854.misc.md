Notes AI routes now await model responses on the request event loop while their synchronous database work runs on worker threads. The background change-summary worker retains synchronous invocation. Model acquisition handles request cancellation so a late worker result receives a cleanup attempt instead of remaining cached and unused. Registered shared model instances remain caller-owned when wrapped or bound to tools; built-in and registered factory results retain close-after-use behavior. Cleanup remains best effort and provider-specific; synchronous cleanup and notes HTTP rate-limit storage checks run on worker threads. The async rate-limit wrapper now scrubs and wraps rate-limit errors; adaptive retry remains disabled in production. Part of #5854; addresses the shared-instance lifetime conflict in #6291.
