The remediation text shown when a research run fails on a missing API key no longer implies a `.env` file is an option — LDR itself reads only the server process's environment. To keep values in a file, hand the file to whatever starts LDR (`docker run --env-file`, Compose `env_file:`, systemd `EnvironmentFile=`).
