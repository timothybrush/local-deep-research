SearXNG search engine rate limiting (`delay_between_requests`) is now shared process-wide per instance URL, ensuring request throttling applies across per-call engine instances constructed during research agent tool calls.
