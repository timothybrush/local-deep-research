A document whose embeddings failed to store is no longer marked as indexed. Previously a chunk-store error left the document flagged indexed but with zero vectors persisted, so it was silently unsearchable and the background reconciler (which only retries un-indexed documents) never picked it up again. The failed index now leaves the document un-indexed so it is retried.
