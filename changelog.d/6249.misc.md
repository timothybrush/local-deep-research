The citation handlers and full-search URL-quality filter gain async counterparts for the upcoming async research runner, while existing callers continue to use the synchronous APIs. Both citation streaming paths now accept message content blocks, preserve text spacing, and retain partial answers after a stream failure. Production async adoption still depends on the client lifecycle and token accounting work in #6293 and #6294. Third tranche of #5854.
