Fixed reports collapsing to just a `## Sources` list when citation synthesis returned empty or unusable text (for example, a reasoning model's citation pass producing only a `<think>` block). The agent's original answer is now preserved beneath a short notice instead of being silently discarded.
