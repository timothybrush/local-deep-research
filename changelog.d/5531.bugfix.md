Enforce the settings lock (`app.lock_settings`) on the delete, import and reset-to-defaults endpoints, which previously ignored it: deleting or bulk-importing settings while locked now returns 403, and reset-to-defaults can no longer clear the lock as a side effect. Startup paths that load newly shipped defaults are unaffected.
