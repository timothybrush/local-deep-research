The path-anchored exemptions in `check-env-vars.py` no longer fail on Windows checkouts where directory separators are backslashes — previously the hook would skip its own exemptions and flag legitimate constant definitions.
