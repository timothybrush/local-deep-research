`close_all_databases` no longer drops per-user init locks, so a concurrent `open_user_database` during shutdown can no longer end up with a second lock for the same user and run two cold-opens against one database file.
