The SearXNG rate limiter now keeps each URL's lock and its last-request time in one tracked entry, so a URL evicted while one of its requests is still in flight can no longer leave its timestamp behind. Tracked state is now bounded by `MAX_TRACKED_URLS` as the module documented, rather than only the locks being bounded.
