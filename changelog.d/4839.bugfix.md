Pressing Enter in a custom model dropdown keeps the typed model name instead of replacing it with the first suggestion. An exact match — a known value first, then a unique label — always wins before any case-insensitive match; known option labels and values also resolve to their model IDs case-insensitively, a padded known value resolves to its option, and a disabled match is not committed.
