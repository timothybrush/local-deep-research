Published source distributions (`.tar.gz`) no longer bundle the repository's `tests/` tree; it stays in Git and CI. Both the wheel and the sdist are now inspected against a size and content policy before upload, rejecting unexpected binaries, development-only files, and unsafe archive entries.
