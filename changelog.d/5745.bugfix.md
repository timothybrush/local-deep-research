A library or collection search that runs under an index configuration the collection was not indexed with no longer hijacks the collection's current index: the empty index row such a search creates now stays non-current, so the real index keeps serving every other search until an actual indexing run switches it.
