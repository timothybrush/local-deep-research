The LangGraph agent's `research_subtopic` tool no longer silently drops inputs beyond the documented "2-5" limit: the cap is lowered from 8 to 5 to match the lead prompt, and any overflow is now surfaced via a logger warning, a `truncated_from` key in progress metadata, and a model-facing note naming the dropped subtopics so the lead agent can re-issue or avoid citing them.
