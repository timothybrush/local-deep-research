`/library/document/...` URL resolution now requires the path segment to be a canonical document id (32/64-hex or UUID), rejecting guessable filenames at the regex instead of relying solely on a downstream database miss.
