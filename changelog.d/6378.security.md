Creating a user database no longer follows a symlink planted at the database path. The path is refused when it is a link, and the file is created exclusively and without following links before SQLCipher or SQLite opens it; a path replaced while the database is being created is detected and the creation fails, although a replacement undone before the check runs is not caught.
