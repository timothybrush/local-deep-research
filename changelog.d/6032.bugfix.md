Journal-quality database sessions are now closed when the calling block raises an unexpected error, so repeated failures no longer strand a SQLite connection and file descriptor. The production case is `InterfaceError` from #5931: it is a `DBAPIError` but not a `DatabaseError`, so it missed the `except (OperationalError, DatabaseError)` arm that used to hold the only close on the error path.
