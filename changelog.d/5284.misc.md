Library searches skip the redundant embedding-dimension probe for faster RAG lookups, while still rejecting queries whose vector dimensions don't match the index — or whose embedding is otherwise degenerate (non-finite, zero-magnitude, or overflow-magnitude) — including when the index is empty; a refused query now raises rather than silently returning no results.
