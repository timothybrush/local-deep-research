Reduce false-positive WARNING/ERROR log noise during normal research runs: filter werkzeug Socket.IO disconnect `write() before start_response` errors, demote DownloadService init path logs to debug, skip the deprecated display-label fallback warning for plain engine keys like `collection_<uuid>`, and include exception context in document-URL warnings.
