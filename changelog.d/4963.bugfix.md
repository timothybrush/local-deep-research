Collection search now ranks results correctly for cosine and dot-product embedding indexes. Previously the relevance score was computed as if every index used L2 distance, which inverted the ranking (the nearest match scored lowest) and could error on an anti-correlated match.
