The "Search everything" page no longer tears down and rebuilds the loading spinner's DOM on every keystroke — consecutive searches while the spinner is already visible reuse the existing nodes (updating only the label if the message changes), reducing per-keystroke DOM churn during fast typing on slow devices.
