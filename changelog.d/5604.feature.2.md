The research form now shows a dismissible warning when SearXNG would silently return no results because its private/localhost instance URL has not been approved by the server operator — including the exact environment line to fix it. A mirror-image warning appears when a local document engine (Paperless-ngx, Elasticsearch) points at a host that does not look private, since document-search queries would then leave the machine.
