Decode OpenAlex snapshot partitions record by record so the journal-quality download no longer holds a whole partition in memory, which could exhaust RAM on smaller hosts and leave the container thrashing in swap.
