Fixed database connection pool exhaustion hangs during deep research runs by releasing checked-out connections on session scope exit, and cached collection search engines with stale-on-error fallback to prevent collection tools from being dropped mid-research.
