The PDF renderer's SSRF guard now logs and raises blocked URLs in redacted form (scheme://host:port only): a rejected URL is adversarial-shaped and may still carry credentials in its userinfo or secret query tokens, which previously landed in server logs and exception chains.
