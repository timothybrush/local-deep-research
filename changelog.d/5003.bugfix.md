Fixed the shared byte-size formatter returning `"NaN undefined"` for negative, `NaN`, or undefined inputs; only finite `number` values are formatted now, and everything else -- including negative, non-finite, and numeric-string inputs like `"1024"` -- renders as `"0 Bytes"`.
