Fix list-form LLM content handling in LangGraph agent strategy consumers to prevent downstream type errors and Python list-representation leakage, including observation, reasoning, final-answer, and fallback synthesis paths.
