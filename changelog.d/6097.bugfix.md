`CollectionSearchEngine` and `LibraryRAGSearchEngine` now load frozen embedding and text splitter configuration directly from the `Collection` record rather than the `RAGIndex` table, ensuring search remains reliable even if indexing was interrupted and resolving text-splitter mismatches across collection queries.
