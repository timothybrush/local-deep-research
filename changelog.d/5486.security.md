Notes API now emits a `logger.warning` audit line when a request is rejected for a non-boolean `is_collapsed`/`force_reindex`/`create_note`, a non-finite `min_similarity`, or a stale `note_id`, giving operators visibility into probing of the tightened input contract (rejected values are logged type-tagged and truncated).
