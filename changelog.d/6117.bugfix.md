Fetching a library document citation with a `#chunk-<n>` anchor now retrieves the cited chunk's text instead of truncating the entire document body. When a cited chunk is missing or empty, the fetch resolver reports the missing chunk or empty content rather than silently falling back to the full document. In the library search engine, chunk retrieval is scoped to the source collection and ordered deterministically.
