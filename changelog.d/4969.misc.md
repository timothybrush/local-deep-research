Missing inline citations in `langgraph-agent` answers are now diagnosable from the server log: a warning fires when a call skips the citation pass despite accumulated sources (the detailed-report subsection case behind #4969), and when a completed synthesis contains no inline `[N]` markers despite available sources. Report content is unchanged.
