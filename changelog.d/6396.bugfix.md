`migrate_legacy_docstores` now reports a failed `chmod(0o700)` of the RAG cache root as `hardening_errors` in its returned stats and in the "did NOT complete cleanly" error log, instead of downgrading it to a warning and reporting the run exactly like a fully clean one.
