Explicit `max_results` and `time_period` overrides are now validated, retain precedence over queued values, and invalid persisted queued values are safely rejected instead of triggering transient retries.
