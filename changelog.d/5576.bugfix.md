Telegram notifications now use Apprise's canonical `tgram://` scheme; the unregistered `telegram://` alias is rejected. Apprise diagnostic log output that could contain notification credentials is suppressed. Multi-target URLs such as `tgram://...?to=id1,id2` are no longer split into partial notifications, and `telegram://` URLs now get a dedicated migration error pointing at the `tgram://` form.
